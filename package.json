{
  "name": "resort-map",
  "version": "1.0.0",
  "description": "Resort Map - Cabana-Booking",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm run start -w backend & npm run start -w frontend",
    "test": "npm test -w backend && npm test -w frontend"
  }
}
